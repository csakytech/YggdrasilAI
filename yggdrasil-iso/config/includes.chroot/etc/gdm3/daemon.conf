# GDM configuration for Yggdrasil / ThorOS.
[daemon]
# Use Xorg, not Wayland, so the OS can position HUDs/overlays and (later) its own desktop
# shell — Wayland forbids apps from self-positioning and staying always-on-top.
WaylandEnable=false
