# --- NVIDIA driver (non-free); RTX 3060 / Ampere. Ollama needs only the driver; GPU Whisper
# uses the pip nvidia-cu12 libs (installed by the app hook), so the 2GB cuda-toolkit is omitted. ---
nvidia-driver
nvidia-kernel-dkms
firmware-misc-nonfree
linux-headers-amd64

# --- Python / app runtime ---
python3
python3-venv
python3-pip
pipx
python3-dev
build-essential

# --- Audio (voice I/O) ---
portaudio19-dev
libasound2-dev
alsa-utils
pipewire
pipewire-audio

# --- Ollama prereqs + tooling ---
curl
ca-certificates
jq
pciutils
git

# --- Desktop control + GTK UIs + office automation (Yggdrasil agents) ---
# NOTE: live-build only strips whole-line comments — never put a # after a package name.
# window activation/close, keystroke injection, focused-window class
wmctrl
xdotool
x11-utils
# PyGObject + GTK3 typelib for the dashboard / HUD / chat window
python3-gi
gir1.2-gtk-3.0
# UNO bridge for the LibreOffice (Writer) deep-adapter agent
python3-uno
